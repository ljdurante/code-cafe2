-- Drops the blogger if it exists currently --
DROP DATABASE IF EXISTS code_cafe;
DROP DATABASE IF EXISTS passport;
-- Creates the "blogger" database --
CREATE DATABASE code_cafe;
CREATE DATABASE passport;